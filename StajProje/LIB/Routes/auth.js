const express = require('express');
const router = express.Router();

// router.post('/signup/:username/:password', (req, res, next) => {
//     const {username, password} = req.params;
//     res.status(200).json({
//         message: 'Signup successful',
//     });
// });

module.exports = router;